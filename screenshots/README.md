# Submission Screenshots

Add these screenshots before submission:

- `dashboard.png`: Render Blueprint/resources showing the web service and Key Value.
- `running.png`: Public `/health` or root endpoint working in a browser.
- `test.png`: Cloud E2E test output showing all tests passed.

Do not include secrets or the value of `AGENT_API_KEY` in screenshots.
